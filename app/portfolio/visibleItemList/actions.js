export const toggleItem = (id) => ({
  type: 'TOGGLE_ITEM',
  id
});

export const setPhoto = (index) => ({
  type: 'SET_PHOTO',
  index
});

export const enlargePhoto = (index) => ({
  type: 'ENLARGE_PHOTO',
  index
});